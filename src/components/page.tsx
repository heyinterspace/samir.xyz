// This file is deprecated and should be removed.
// The root route is now handled by /src/app/page.tsx
// which serves the Profile component as the index page.

// Removing redirect since it's no longer needed - the App Router handles the root route
export { default } from '../app/page';